# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

<% 5.times do |i| %>
user_<%= i %>:
  email: "user_<%= i %>@<%= Rails.application.class.name.split('::').first.downcase %>"
  encrypted_password: <%= Devise::Encryptor.digest(User, "password#{i}") %>
<% end %>

<% 5.times do |i| %>
oauthed_<%= i %>:
  email: "oauthed_<%= i %>@<%= Rails.application.class.name.split('::').first.downcase %>"
  encrypted_password: <%= Devise.friendly_token %>
<% end %>

orphans: 
  email: "orphans@<%= Rails.application.class.name.split('::').first.downcase %>"
  encrypted_password: <%= Devise.friendly_token %>
